{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur{% endblock %}

{% block body %}
    <div class="min-vh-100"></div>
    <main class="container d-flex flex-column align-items-center" id="user-admin-edit">
        {% for category, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ category }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
        <h1 class="text-center my-3">Modification d'un utilisateur</h1>
        {{ include('user_admin/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        {{ include('user_admin/_delete_form.html.twig') }}
        <a class="btn btn-outline-primary rounded-pill mb-3" href="{{ path('app_user_admin_index', {'_fragment': 'user-index'}) }}">Retour</a>
    </main>
{% endblock %}
